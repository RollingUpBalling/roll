<!DOCTYPE html>
<html>

<head>
  <title>Authenticated</title>
</head>

<body>
  Authenticated successfully.

  <script>
    console.log('<%= jwtToken %>')
    window.opener.postMessage({
      token: '<%= jwtToken %>',
      ok: true,
      id: '<%= id %>',
      username: '<%= username %>'
    }, '<%= clientUrl %>');

    window.close();
  </script>
</body>

</html>